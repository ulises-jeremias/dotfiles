pkg_install() {
    if [ -x "${ROOT}/debian/$1" ]; then
        ${ROOT}/debian/$1
        return $?
    fi
    sudo apt install $1
    return $?
}

pkg_exists() {
    [ -f "${ROOT}/debian/$1" ] || apt search $1
    return $?
}
