/* ============================================================================
 * Waybar i3-Style Configuration - Smart Colors Edition (Cozy Variant)
 * ============================================================================
 * Clean duplications and avoid parsing errors.
 * Palette: neutral base, moderate accents, highlighted states.
 * ============================================================================ */

@import "{{ joinPath .chezmoi.homeDir ".cache/wal/colors-waybar.css" }}";
@import "{{ joinPath .chezmoi.homeDir ".cache/dots/smart-colors/colors-waybar.css" }}";

/* ==========================================================================
 * Global
 * ==========================================================================
 */
* {
	border: none;
	border-radius: 0;
	font-family: "Hack Nerd Font Mono", "Noto Color Emoji", monospace;
	font-size: 13px;
	font-weight: normal;
	min-height: 0;
}

/* Increase icon size for Nerd Fonts without affecting text/numbers */
#workspaces button,
#custom-jgmenu,
#custom-apps,
#custom-window-switch,
#custom-rices,
#custom-audio-header,
#custom-tools-header {
	font-size: 22px;
}

/* Icon size for modules with icons + text */
#pulseaudio,
#pulseaudio.microphone,
#backlight,
#custom-night-mode,
#custom-github,
#custom-weather,
#battery,
#custom-performance-profile {
	font-size: 15px;
}

.text-small {
	font-size: 10px;
}

.text-normal {
	font-size: 12px;
}

.text-medium {
	font-size: 13px;
}

.text-large {
	font-size: 15px;
}

.text-xlarge {
	font-size: 18px;
}

/* ==========================================================================
 * Windows
 * ==========================================================================
 */
window#waybar.top {
	background-color: @bg-primary;
	color: @fg-primary;
	border-radius: 12px;
}

window#waybar.bottom {
	background-color: @bg-primary;
	color: @fg-primary;
	border-radius: 12px;
	border: 1px solid rgba(255, 255, 255, 0.1);
}

/* ==========================================================================
 * Workspaces
 * ==========================================================================
 */
#workspaces {
	background: transparent;
	margin: 0;
	padding: 0;
}

#workspaces button {
	background: transparent;
	color: @fg-primary;
	padding: 2px 12px;
	margin: 4px 2px;
	border: 1px solid transparent;
	border-radius: 6px;
	transition: all 0.2s ease;
}

#workspaces button:hover {
	background-color: @bg-hover;
	color: @smart-cyan;
	border-color: @smart-cyan;
}

#workspaces button.active {
	background-color: rgba(249, 181, 90, 0.2);
	color: @smart-orange;
	border-color: @smart-orange;
	border-width: 2px;
	font-weight: bold;
	box-shadow: 0 0 8px rgba(249, 181, 90, 0.3);
}

#workspaces button.empty {
	opacity: 0.4;
	color: @fg-secondary;
}

#workspaces button.empty:hover {
	opacity: 0.7;
}

#workspaces button.urgent {
	background-color: @smart-error;
	color: @bg-primary;
}

@keyframes blink {
	0% {
		opacity: 1;
	}

	50% {
		opacity: 0.55;
	}

	100% {
		opacity: 1;
	}
}

@keyframes pulse {
	0% {
		opacity: 1;
	}

	50% {
		opacity: 0.7;
	}

	100% {
		opacity: 1;
	}
}

/* ==========================================================================
 * Custom Modules
 * ==========================================================================
 */

/* JGMenu */
#custom-jgmenu {
	color: @smart-cyan;
	padding: 0 12px;
	margin: 0 4px;
	border: 1px solid transparent;
	border-radius: 6px;
	transition: all 0.2s ease;
}

#custom-jgmenu:hover {
	background-color: @bg-hover;
	border-color: @smart-cyan;
}

/* ==========================================================================
 * Apps Groups - Quick Actions Menu
 * ==========================================================================
 */
box#quick-apps,
box#system-apps,
box#power-actions,
box#tools {
	padding: 4px 8px;
	margin: 0 4px;
	border-radius: 8px;
}

box#quick-apps:hover,
box#system-apps:hover,
box#power-actions:hover,
box#tools:hover {
	background-color: @bg-hover;
}

#custom-quick-apps-header {
	color: @smart-cyan;
	font-size: 22px;
	padding: 0 8px;
}

#custom-system-apps-header {
	color: @smart-orange;
	font-size: 22px;
	padding: 0 8px;
}

#custom-power-actions-header {
	color: @smart-error;
	font-size: 22px;
	padding: 0 8px;
}

#custom-tools-header {
	color: @smart-magenta;
	font-size: 22px;
	padding: 0 8px;
}

/* Quick Apps - Category 1 (Primary actions) */
#custom-apps-web {
	color: @smart-cyan;
	padding: 0 8px;
	border: 1px solid transparent;
	border-radius: 6px;
	transition: all 0.2s ease;
}

#custom-apps-terminal {
	color: @smart-green;
	padding: 0 8px;
	border: 1px solid transparent;
	border-radius: 6px;
	transition: all 0.2s ease;
}

#custom-apps-files {
	color: @smart-yellow;
	padding: 0 8px;
	border: 1px solid transparent;
	border-radius: 6px;
	transition: all 0.2s ease;
}

/* System Tools - Category 2 */
#custom-apps-system {
	color: @smart-orange;
	padding: 0 8px;
	border: 1px solid transparent;
	border-radius: 6px;
	transition: all 0.2s ease;
}

#custom-apps-settings {
	color: @smart-orange;
	padding: 0 8px;
	border: 1px solid transparent;
	border-radius: 6px;
	transition: all 0.2s ease;
}

#custom-apps-bluetooth {
	color: @smart-cyan;
	padding: 0 8px;
	border: 1px solid transparent;
	border-radius: 6px;
	transition: all 0.2s ease;
}

#custom-apps-theme {
	color: @smart-magenta;
	padding: 0 8px;
	border: 1px solid transparent;
	border-radius: 6px;
	transition: all 0.2s ease;
}

#custom-apps-audio {
	color: @smart-pink;
	padding: 0 8px;
	border: 1px solid transparent;
	border-radius: 6px;
	transition: all 0.2s ease;
}

/* Power Actions - Category 3 */
#custom-apps-lock {
	color: @fg-secondary;
	padding: 0 8px;
	border: 1px solid transparent;
	border-radius: 6px;
	transition: all 0.2s ease;
}

#custom-apps-reboot {
	color: @smart-warning;
	padding: 0 8px;
	border: 1px solid transparent;
	border-radius: 6px;
	transition: all 0.2s ease;
}

#custom-apps-shutdown {
	color: @smart-error;
	padding: 0 8px;
	border: 1px solid transparent;
	border-radius: 6px;
	transition: all 0.2s ease;
}

/* Hover states for app buttons */
#custom-apps-web:hover {
	background-color: @bg-hover;
	border-color: @smart-cyan;
}

#custom-apps-terminal:hover {
	background-color: @bg-hover;
	border-color: @smart-green;
}

#custom-apps-files:hover {
	background-color: @bg-hover;
	border-color: @smart-yellow;
}

#custom-apps-system:hover {
	background-color: @bg-hover;
	border-color: @smart-orange;
}

#custom-apps-settings:hover {
	background-color: @bg-hover;
	border-color: @smart-orange;
}

#custom-apps-bluetooth:hover {
	background-color: @bg-hover;
	border-color: @smart-cyan;
}

#custom-apps-theme:hover {
	background-color: @bg-hover;
	border-color: @smart-magenta;
}

#custom-apps-audio:hover {
	background-color: @bg-hover;
	border-color: @smart-pink;
}

#custom-apps-lock:hover {
	background-color: @bg-hover;
	border-color: @fg-secondary;
}

#custom-apps-reboot:hover {
	background-color: rgba(249, 181, 90, 0.15);
	border-color: @smart-warning;
}

#custom-apps-shutdown:hover {
	background-color: rgba(235, 103, 152, 0.15);
	border-color: @smart-error;
}

/* Tools - Category 4 */
#custom-tools-clipboard,
#custom-tools-screenshot,
#custom-tools-rice,
#custom-tools-monitors,
#custom-tools-security {
	color: @smart-green;
	padding: 0 8px;
	margin: 0 4px;
	border: 1px solid transparent;
	border-radius: 6px;
	transition: all 0.2s ease;
}

#custom-tools-screenshot {
	color: @smart-cyan;
}

#custom-tools-rice {
	color: @smart-magenta;
}

#custom-tools-monitors {
	color: @smart-orange;
}

#custom-tools-security {
	color: @smart-pink;
}

#custom-tools-keyboard-help {
	color: @smart-yellow;
	padding: 0 8px;
	margin: 0 4px;
	border: 1px solid transparent;
	border-radius: 6px;
	transition: all 0.2s ease;
}

#custom-tools-clipboard:hover,
#custom-tools-screenshot:hover,
#custom-tools-rice:hover,
#custom-tools-monitors:hover,
#custom-tools-security:hover {
	background-color: @bg-hover;
	border-color: currentColor;
}

#custom-tools-keyboard-help:hover {
	background-color: @bg-hover;
	border-color: @smart-yellow;
}

/* Clipboard Quick Access */
#custom-clipboard-quick {
	color: @smart-green;
	padding: 2px 10px;
	margin: 4px 2px;
	border: 1px solid transparent;
	border-radius: 6px;
	transition: all 0.2s ease;
}

#custom-clipboard-quick:hover {
	background-color: @bg-hover;
	border-color: @smart-green;
}

#custom-separator-dot {
	color: @fg-tertiary;
	font-size: 10px;
	padding: 0 4px;
}

/* Old custom modules (keeping for compatibility) */
#custom-jgmenu,
#custom-apps,
#custom-window-switch,
#custom-rices {
	background: transparent;
	padding: 2px 12px;
	margin: 4px 2px;
	border: 1px solid transparent;
	border-radius: 6px;
	transition: all 0.2s ease;
}

#custom-jgmenu {
	color: @smart-cyan;
}

#custom-apps {
	color: @smart-pink;
}

#custom-window-switch {
	color: @smart-yellow;
}

#custom-rices {
	color: @smart-magenta;
}

#custom-jgmenu:hover,
#custom-apps:hover,
#custom-window-switch:hover,
#custom-rices:hover {
	background-color: @bg-hover;
	border-color: currentColor;
}

/* ==========================================================================
 * Audio & Brightness Modules
 * ==========================================================================
 */
/* Volume and Brightness groups */
box#volume,
box#brightness {
	background: transparent;
	padding: 2px 10px;
	margin: 4px 2px;
	border: 1px solid transparent;
	border-radius: 6px;
	transition: all 0.2s ease;
}

#pulseaudio {
	color: @smart-orange;
}

#pulseaudio.microphone {
	color: @smart-pink;
}

box#volume:hover {
	background-color: @bg-hover;
	border-color: @smart-orange;
}

box#brightness:hover {
	background-color: @bg-hover;
	border-color: @smart-yellow;
}

/* Microphone (standalone) */
#pulseaudio.microphone {
	background: transparent;
	color: @smart-pink;
	padding: 2px 10px;
	margin: 4px 2px;
	border: 1px solid transparent;
	border-radius: 6px;
	transition: all 0.2s ease;
}

#pulseaudio.microphone:hover {
	background-color: @bg-hover;
	border-color: @smart-pink;
}

/* Volume and Backlight modules inside groups */
box#volume > #pulseaudio,
box#brightness > #backlight {
	background: transparent;
	padding: 2px 8px;
	margin: 0;
	border: none;
	color: @smart-orange;
}

box#brightness > #backlight {
	color: @smart-yellow;
}

#pulseaudio.muted,
#pulseaudio.microphone.source-muted {
	color: @smart-warning;
}

/* ==========================================================================
 * Clock
 * ==========================================================================
 */
#clock {
	background: rgba(255, 255, 255, 0.05);
	color: @fg-primary;
	padding: 2px 16px;
	margin: 4px 4px;
	border: 1px solid rgba(255, 255, 255, 0.1);
	border-radius: 6px;
	font-weight: bold;
	font-size: 13px;
}

#clock:hover {
	background-color: @bg-hover;
	border-color: @smart-pink;
}

/* ==========================================================================
 * System Monitors & Controls
 * ==========================================================================
 */
#disk,
#cpu,
#memory,
#temperature,
#custom-night-mode,
#custom-github,
#custom-weather,
#battery,
#custom-performance-profile {
	background: transparent;
	padding: 2px 10px;
	margin: 4px 2px;
	border: 1px solid transparent;
	border-radius: 6px;
	transition: all 0.2s ease;
}

#disk {
	color: @smart-magenta;
}

#cpu {
	color: @smart-cyan;
}

#memory {
	color: @smart-yellow;
}

#temperature {
	color: @smart-green;
}

#temperature.critical {
	color: @smart-error;
	animation: blink 1s ease infinite;
}

#disk:hover,
#cpu:hover,
#memory:hover,
#temperature:hover {
	background-color: @bg-hover;
	border-color: currentColor;
}

#custom-night-mode {
	color: @smart-yellow;
}

#custom-github {
	color: @smart-green;
}

#custom-weather {
	color: @smart-cyan;
}

#battery {
	color: @smart-orange;
}

#custom-performance-profile {
	color: @smart-magenta;
}

#custom-night-mode:hover,
#custom-github:hover,
#custom-weather:hover,
#battery:hover,
#custom-performance-profile:hover {
	background-color: @bg-hover;
	border-color: currentColor;
}

#battery.charging {
	color: @smart-green;
}

#battery.plugged {
	color: @smart-cyan;
}

#battery.warning:not(.charging) {
	color: @smart-warning;
	animation: pulse 1s ease infinite;
}

#battery.critical:not(.charging) {
	color: @smart-error;
	animation: blink 1s ease infinite;
}

/* ==========================================================================
 * Keyboard Layout
 * ==========================================================================
 */
#language {
	background: transparent;
	padding: 2px 10px;
	margin: 4px 2px;
	border: 1px solid transparent;
	border-radius: 6px;
	font-size: 16px;
	transition: all 0.2s ease;
}

#language:hover {
	background-color: @bg-hover;
	border-color: rgba(255, 255, 255, 0.1);
}

/* ==========================================================================
 * Tray
 * ==========================================================================
 */
#tray {
	background: transparent;
	padding: 2px 8px;
	margin: 4px 2px;
	border: 1px solid transparent;
	border-radius: 6px;
}

#tray:hover {
	background-color: @bg-hover;
	border-color: rgba(255, 255, 255, 0.1);
}

#tray>.passive {
	-gtk-icon-effect: dim;
}

#tray>.needs-attention {
	-gtk-icon-effect: highlight;
	animation: pulse 1s ease infinite;
}

/* ==========================================================================
 * Media Player
 * ==========================================================================
 */
#mpris,
#custom-playing,
#custom-player-controls,
#custom-player-info {
	background: transparent;
	padding: 2px 10px;
	margin: 4px 2px;
	border: 1px solid transparent;
	border-radius: 6px;
	transition: all 0.2s ease;
}

#custom-player-controls {
	font-size: 13px;
	color: @smart-magenta;
}

#custom-player-controls:hover {
	background-color: @bg-hover;
	border-color: @smart-magenta;
}

#custom-player-info {
	color: @fg-primary;
	font-size: 12px;
}

#custom-player-info:hover {
	background-color: @bg-hover;
	border-color: @smart-cyan;
}

/* ==========================================================================
 * Hyprland Window Module
 * ==========================================================================
 */
#window {
	background: transparent;
	color: @fg-primary;
	padding: 2px 12px;
	margin: 4px 4px;
	border: 1px solid transparent;
	border-radius: 6px;
	font-size: 12px;
	transition: all 0.2s ease;
}

#window:hover {
	background-color: @bg-hover;
	border-color: @smart-accent;
}

#window.empty {
	opacity: 0.5;
}

/* ==========================================================================
 * Hyprland Window Module
 * ==========================================================================
 */
#window {
	background: transparent;
	color: @fg-primary;
	padding: 2px 12px;
	margin: 4px 4px;
	border: 1px solid transparent;
	border-radius: 6px;
	font-size: 12px;
	transition: all 0.2s ease;
}

#window:hover {
	background-color: @bg-hover;
	border-color: @smart-accent;
}

#window.empty {
	opacity: 0.5;
}

/* ==========================================================================
 * System Resources
 * ==========================================================================
 */
#language {
	background: transparent;
	color: @smart-cyan;
	padding: 2px 10px;
	margin: 4px 2px;
	border: 1px solid transparent;
	border-radius: 6px;
	font-size: 12px;
	transition: all 0.2s ease;
}

#language:hover {
	background-color: @bg-hover;
	border-color: @smart-cyan;
}

#disk {
	background: transparent;
	color: @smart-magenta;
	padding: 2px 10px;
	margin: 4px 2px;
	border: 1px solid transparent;
	border-radius: 6px;
	font-size: 12px;
	transition: all 0.2s ease;
}

#disk:hover {
	background-color: @bg-hover;
	border-color: @smart-magenta;
}

#memory {
	background: transparent;
	color: @smart-yellow;
	padding: 2px 10px;
	margin: 4px 2px;
	border: 1px solid transparent;
	border-radius: 6px;
	font-size: 12px;
	transition: all 0.2s ease;
}

#memory:hover {
	background-color: @bg-hover;
	border-color: @smart-yellow;
}

#cpu {
	background: transparent;
	color: @smart-pink;
	padding: 2px 10px;
	margin: 4px 2px;
	border: 1px solid transparent;
	border-radius: 6px;
	font-size: 12px;
	transition: all 0.2s ease;
}

#cpu:hover {
	background-color: @bg-hover;
	border-color: @smart-pink;
}

#temperature {
	background: transparent;
	color: @smart-green;
	padding: 2px 10px;
	margin: 4px 2px;
	border: 1px solid transparent;
	border-radius: 6px;
	font-size: 12px;
	transition: all 0.2s ease;
}

#temperature:hover {
	background-color: @bg-hover;
	border-color: @smart-green;
}

#disk.warning,
#memory.warning,
#cpu.warning {
	color: @smart-warning;
	animation: pulse 1s ease infinite;
}

#disk.critical,
#memory.critical,
#cpu.critical,
#temperature.critical {
	color: @smart-error;
	animation: blink 1s ease infinite;
}

/* ==========================================================================
 * Sliders (Backlight & PulseAudio)
 * ==========================================================================
 * Styling for slider modules. GTK requires explicit min-width/height.
 */
#backlight-slider,
#pulseaudio-slider {
	background: transparent;
	padding: 2px 6px;
	margin: 4px 2px;
	border: 1px solid transparent;
	border-radius: 6px;
}

#backlight-slider trough,
#pulseaudio-slider trough {
	min-height: 8px;
	min-width: 80px;
	border-radius: 4px;
	background-color: @bg-hover;
	border: 1px solid rgba(255, 255, 255, 0.05);
}

#backlight-slider highlight {
	border-radius: 4px;
	background-color: @smart-yellow;
}

#pulseaudio-slider highlight {
	border-radius: 4px;
	background-color: @smart-orange;
}

#backlight-slider slider,
#pulseaudio-slider slider {
	min-width: 0;
	min-height: 0;
	opacity: 0;
	background: none;
	border: none;
	box-shadow: none;
}
