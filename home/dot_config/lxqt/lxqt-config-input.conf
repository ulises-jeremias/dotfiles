# LXQt Keyboard Configuration
# Managed by lxqt-config-input
# This file stores window geometry and UI preferences only
# Actual keyboard settings are managed by the system

[General]
__userfile__=true
