{{ if and (not .ephemeral) (not .headless) -}}

{{ if eq .osid "linux-arch" -}}
#!/usr/bin/env bash

yay -S --noconfirm --needed \
  feh \
  python-pywal \
  sassc

# --force replaces legacy pipx-managed wpg binaries in ~/.local/bin.
# Always reinstall with --with pygobject so upgrades keep the dependency.
uv tool install --force --with pygobject wpgtk
{{ end -}}

{{ end -}}
