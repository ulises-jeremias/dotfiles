name: Todo Checker

on:
  workflow_call:

permissions:
  contents: read

jobs:
  todo:
    name: TODO

    runs-on: ubuntu-latest

    permissions:
      contents: read
      issues: write

    steps:
      - name: Begin CI...
        uses: actions/checkout@v4

      - name: TODO to Issue
        uses: alstr/todo-to-issue-action@v4
        id: todo
